# apaga
# seja uma string palavra e um inteiro index
# retorna uma nova string sem a posição index
# retorna a palavra completa quando o index for maior que o tamanho da palavra
# apaga('kitten', 1) -> 'ktten'
# apaga('kitten', 4) -> 'kittn'
